#include <math.h>
#include "rectangle.h"
#include <stdio.h>

double rectangle_method(double a,double b, int step)
{
double rectangle_integral;

    step = 2 * (a + b);
    rectangle_integral = a * b;

    
return 0 ;
}